<%= form_with(model: area, url: [room, area], remote: true, html: {name: "areaForm", class: "p-2"} ) do |form| %>
  <% if area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>
      <ul>
        <% ares.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.text_area :content, rows: 4 %>
  </div>
  <div class="actions text-right">
    <%= link_to "Close", new_room_area_path(room), class: "btn btn-secondary", remote: true %>
    <%= form.submit %>
  </div>
<% end %>